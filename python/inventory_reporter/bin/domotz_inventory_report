#!/usr/bin/env python3
from os.path import join, dirname
import sys

sys.path.insert(0, join(dirname(__file__), '..', 'src'))

from inventory_reporter.reporter import Runner

if __name__ == '__main__':
    Runner.main()
